{
  "name": "ems-web-app-utils",
  "author": "Chris Spence",
  "version": "0.0.13",
  "peerDependencies": {
    "@angular/common": "^13.2.0",
    "@angular/core": "^13.2.0",
    "underscore": "^1.13.4"
  },
  "dependencies": {
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@types/underscore": "^1.11.4"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/spencech/ems-web-app-utils.git"
  },
  "scripts": {
    "publish": "git add -A . && git commit -m 'prepublish npm package' && npm version patch && ng build utils && cd dist && npm publish"
  }
}
